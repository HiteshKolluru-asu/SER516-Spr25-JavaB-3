server:
  port: 8080

spring:
  application:
    name: routing-service
#  cloud:
#    gateway:
#      discovery:
#        locator:
#          enabled: true
#          lowerCaseServiceId: true
#      routes:
#        - id: afferent-coupling
#          uri: lb://AFFERENT-COUPLING-API  # ✅ Eureka service name
#          predicates:
#            - Path=/api/afferent-coupling/**
#
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://eureka-server:8761/eureka
#    register-with-eureka: true
#    fetch-registry: true
#  instance:
#    instance-id: ${spring.application.name}:${server.port}  # Unique instance ID
#    leaseRenewalIntervalInSeconds: 10  # Send heartbeat every 10s
#    leaseExpirationDurationInSeconds: 30  # Wait 30s before removing service
#
